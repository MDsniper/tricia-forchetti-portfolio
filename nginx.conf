server {
    listen 8080;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(js|jsx|css|png|jpg|jpeg|gif|svg|ico)$ {
        expires -1;
        add_header Cache-Control "no-store, no-cache, must-revalidate";
    }
}